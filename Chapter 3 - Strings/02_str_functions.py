#To find length of the string
name = 'hamza'
print(len(name))

#to find if the length ends with a particular character
print(name.endswith('a'))

#to find the length of the string starts with
print(name.startswith('h'))

#Rememeber starts with and ends with are case sensitive, Small or Capital letters would be treated differently.

## To capitalize the string
print(name.capitalize())


