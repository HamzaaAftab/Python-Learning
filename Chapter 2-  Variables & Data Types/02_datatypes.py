a=1 # It is number type
 
b=5.22 # It is float type

c='Harry' # It is string type

d=True # It is boolean type (Yes or No only)

e = None # It is a none type variable, Why we are using None? Sometimes we need to mark that this variable stores nothing, then we use None. It indicates that this variable have nothing..

print( type(a), type(b), type(c), type(d), type(e)) # Print to check type 

##  RULES FOR DEFINING VARIABLE NAME

# 1. Variable name should not start with number
# 2. Variable name should not contain any special character except underscore (_)
# 3. Variable name should not be keyword
# 4. Variable name should be meaningful